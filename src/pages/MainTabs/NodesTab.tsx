/**
 * Nodes tab — peer list, discovery, PIN pairing, cloud status.
 */
import React from 'react';
import { NodeList } from '../NodeList';

export function NodesTab(): React.JSX.Element {
  return <NodeList />;
}
